routes:
  - match: { topic: demo }
    response:
      body:
        event: demo
        message: SSE demo topic

  - match: { topic: orders.created }
    response:
      body:
        event: orders.created
        message: Order created event stream
