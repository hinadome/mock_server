# Optional: expose MQTT cleartext on 1883 (often conflicts with other brokers).
# Enabled only when deploy.sh is run with --mqtt-cleartext

server {
    listen 1883;
    listen [::]:1883;
    proxy_pass mock_mqtt;
    proxy_connect_timeout 10s;
    proxy_timeout 1h;
}
