# Map for WebSocket Connection header (include inside http {} block)
# Already common on Ubuntu nginx; deploy script ensures this snippet exists.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
