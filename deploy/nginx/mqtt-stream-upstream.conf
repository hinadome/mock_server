# Shared MQTT upstream for stream proxies (cleartext and/or TLS).
# App listens on 127.0.0.1:11883 (internal); nginx publishes :1883 / :8883.
# Using a different internal port avoids EADDRINUSE with nginx on the host.

upstream mock_mqtt {
    server 127.0.0.1:11883;
}
