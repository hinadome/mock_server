# Optional MQTT cleartext on :1883 (opt-in via deploy.sh --mqtt-cleartext)
server {
    listen 1883;
    listen [::]:1883;
    proxy_pass mock_mqtt;
    proxy_connect_timeout 10s;
    proxy_timeout 1h;
}
